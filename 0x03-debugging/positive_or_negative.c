#include "main.h"

/**
 * main - determine if a num is pos,neg or zero
 * 0 : the number to be checked
 * Return: Always 0 on Success
 */
void positive_or_negative(int i)
{
	if (n < 0)
{
	printf("%d is %s\n", n, "negative");
}
	else if (n > 0)
{
	printf("%d is %s\n", n, "positive");
}
	else
{
	printf("%d is %s\n", n, "zero");
}
	return (0);
}
