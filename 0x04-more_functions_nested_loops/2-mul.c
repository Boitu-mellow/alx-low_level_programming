/**
 *mul - function that multiplies two integers.
 *@x:integer to multiply the second
 *@y:integer to multiply the first
 *Return: the product of multiplicatio
 */

int mul(int x, int y)
{
	return (x * y);
}
